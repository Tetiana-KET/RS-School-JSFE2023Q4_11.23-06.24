import './normalize.css';
import './style.css';
// import minesweeper from './assets/images/minesweeper.jpg';

// const hello = require('./hello');

// const imgMine = document.getElementById('imgId');
// imgMine.src = minesweeper;

/**
 * burger
 * document.addEventListener('click', toggleBurgerMenu);

  function toggleBurgerMenu(e) {
    const targetItem = e.target;

    if(targetItem.closest('.burger-menu')) {
      document.documentElement.classList.toggle('menu-open');
    }
  }
 */
